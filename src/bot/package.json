{
  "name": "kawkaw-bot",
  "version": "1.0.0",
  "description": "KawKaw relay bot — reads chat, runs the meter engine, broadcasts to Twitch Extension overlays.",
  "scripts": {
    "dev": "nodemon bot.js",
    "start": "node bot.js"
  },
  "dependencies": {
    "dotenv": "^16.4.1",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
